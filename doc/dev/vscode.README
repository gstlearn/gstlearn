This document describes how to upgrade VSCODE (for dummy user)

When a new upgrade is available (bouton left button witho VSCODE):
- press the "wheel"
- follow the link and upload the DEB file
- log out from Vscode
- login as a sudoer and type:

sudo dpkg -i file.deb

Finished.

